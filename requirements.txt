# PyTorch is intentionally absent. On NVIDIA Linux, install the CUDA-enabled
# wheel selected at https://pytorch.org/get-started/locally/ before this file.
diffusers>=0.30.0,<1.0
transformers>=4.41.0,<5.0
accelerate>=0.30.0,<2.0
safetensors>=0.4.3,<1.0
Pillow>=10.3.0,<13.0

